asyncpg==0.30.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
pydantic==2.10.4
python-multipart==0.0.20
argon2-cffi==23.1.0
croniter==6.2.4
PyYAML==6.0.3
# Runtime since automations: an http_request action is an outbound call the
# server makes itself, so httpx is no longer only the test client.
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.0
