"""Federation: upstream MCP servers as connections (spec/agent-auth-plane/05).

`client`    -- the streamable-HTTP MCP client the gateway is to an upstream
`guards`    -- the argument-guard grammar, pure
`catalogue` -- the cached tool catalogue and its refresh
`routes`    -- the read routes behind the MCP Servers screen
"""
