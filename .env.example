# Database
DATABASE_URL=postgresql://datumsync:datumsync_local@localhost:5435/datumsync

# Server
HOST=0.0.0.0
PORT=8200

# Paths (relative to repo root unless absolute)
REPOSITORIES_PATH=./repositories
RESOURCES_PATH=./resources
DATA_PATH=./data
