# Audit mirror verification — 2026-09-13T20:22:14+00:00, last 90 days

Generated by `tools/verify_audit_mirror.py` against this box's database before migration 023 dropped the mirrors. A mirror row *matches* when an `audit_log` row has the same actor (or, for an agent, the sponsor in `detail.account`), verb, minute and target; it *agrees* when outcome, governance and error code match too.

| Table | Rows | Matched in audit_log | Agree on outcome/governance/error | audit_log rows for the same verbs |
|---|---|---|---|---|
| `mcp_call_log` | 908 | 908 | 908 | 2639 |
| `proxy_log` | 0 | 0 | — | 18 |

**Differences explained:**

- `audit_log` rows with outcome `denied` in the window: 102. The mirror recorded a refused federated call as `ok` (the tool result carried `isError`), which is the disagreement D-18 named as the reason to retire it.
- `audit_log` rows for verbs the mirror never had: `rest` 2312, `federate` 933, `oauth` 614, `principal` 516, `enrol` 280, `ui` 79, `auth` 56.
- `mcp_call_log` rows the pairing could not match: 0.

**Verdict:** every mirror row has an audit row; the mirrors can be dropped.
